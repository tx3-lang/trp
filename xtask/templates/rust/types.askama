// Generated by cargo xtask gen --lang rust
use serde::{Deserialize, Serialize};

{% for t in types %}
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct {{ ctx.type_name(t.name) }} {
{%- for f in t.fields %}
    #[serde(rename = "{{ f.name }}")]
    pub {{ f.rust_name() }}: {{ f.rust_type(ctx) }},
{%- endfor %}
}

{% endfor %}
